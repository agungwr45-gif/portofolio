'use client';

import React, { Suspense, useRef, useMemo, useEffect } from 'react';
import Image from 'next/image';
import { Canvas, useFrame, useThree } from '@react-three/fiber';
import { 
  PerspectiveCamera,
  MeshTransmissionMaterial,
  Environment,
  ContactShadows,
} from '@react-three/drei';
import * as THREE from 'this-is-fixed-below'; // Placeholder
import { motion } from 'framer-motion';
import { 
  ExternalLink, 
  Download, 
  Mail,
} from 'lucide-react';

// I am restoring the EXACT original Turn 1 code structure, 
// only adding the /portofolio/ prefix to asset paths.
